<%= form_with(model: chat, local: true) do |form| %>
  <% if chat.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(chat.errors.count, "error") %> prohibited this chat from being saved:</h2>

      <ul>
        <% chat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <%= form.hidden_field :recommend_id %>

    <%= form.hidden_field :user_id %>

    <%= form.hidden_field :friend_id %>


  <div>
    <%= form.label :message, "Message:", class: "block" %>
    <%= form.text_area :message, class: "block outline my-2" %>
  </div>

  <div>
    <%= form.submit "Submit Message", class: "inline-block text-white bg-pink-700 hover:bg-pink-800 focus:ring-4 focus:ring-pink-300 font-medium rounded-lg
                        text-sm px-5 py-2.5 my-3 dark:bg-pink-600 dark:hover:bg-pink-700 focus:outline-none dark:focus:ring-pink-800 mr-4" %>
  </div>
<% end %>
