<%= form_with(model: season_viewing) do |form| %>
  <% if season_viewing.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(season_viewing.errors.count, "error") %> prohibited this season_viewing from being saved:</h2>

      <ul>
        <% season_viewing.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
    <% if action_name == 'new' %>
    <%# hidden fields %>
    <%= form.hidden_field :tmdb_id, value: params[:tmdb_id].to_i %>
    <%= form.hidden_field :content_id, value: params[:content_id].to_i %>
    <%= form.hidden_field :viewing_id, value: params[:viewing_id].to_i %>
    <%= form.hidden_field :show_id, value: params[:show_id].to_i %>
    <%# eventually add season model, but no need to set that up yet %>
    <% end %>
  <div>
    <%= form.label :note, style: "display: block" %>
    <%= form.text_area :note, class: "mb-3 bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5
      dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500" %>
  </div>

  <div>
    <%= form.label :status, "Not Started", style: "display: block" %>
    <%= form.radio_button :status, 'not_started' %>
  </div>
  <div>
    <%= form.label :status, "Watching", style: "display: block" %>
    <%= form.radio_button :status, 'watching' %>
  </div>
  <div>
    <%= form.label :status, "On Hold", style: "display: block" %>
    <%= form.radio_button :status, 'on_hold' %>
  </div>
  <div>
    <%= form.label :status, "Finished", style: "display: block" %>
    <%= form.radio_button :status, 'finished' %>
  </div>

  <div>
    <%= form.submit class: "inline-block text-white bg-pink-700 hover:bg-pink-800 focus:ring-4 focus:ring-pink-300 font-medium rounded-lg
                        text-sm px-5 py-2.5 my-3 dark:bg-pink-600 dark:hover:bg-pink-700 focus:outline-none dark:focus:ring-pink-800"  %>
  </div>
<% end %>
