<% if notice %>
    <div class="container mx-auto py-5 flex">
      <p class="text-green-500"><%= notice %></p>
    </div>
<% end %>
<% if alert %>
  <div class="container mx-auto py-5 flex">
    <p class="text-red-500"><%= alert %></p>
  </div>
<% end %>